export enum DraggableType {
  ICON = 'Icon',
  TEXT = 'Text',
  CONTAINER = 'Container',
}

export enum ButtonType {
  PRIMARY = 'PRIMARY',
  SECONDARY = 'SECONDARY',
  DANGER = 'DANGER',
}

export enum MenuType {
  NEW = 'NEW',
  EDIT = 'EDIT',
}
